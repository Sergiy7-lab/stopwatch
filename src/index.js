import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <BrowserRoute basename={pocess.env}>
    <App />, document.getElementById("root")
  </BrowserRoute>
);
